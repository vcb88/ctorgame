{
  "private": true,
  "scripts": {
    "build": "pnpm -r build",
    "dev": "pnpm -r --parallel dev",
    "lint": "pnpm -r lint",
    "clean": "rm -rf node_modules .pnpm-store coverage .nyc_output && pnpm -r clean && find . -type d \\( -name '.cache' -o -name 'dist' -o -name 'build' -o -name 'node_modules' -o -name 'coverage' -o -name '.nyc_output' \\) -exec rm -rf {} + && find . -name 'tsconfig.tsbuildinfo' -type f -delete"
  },
  "devDependencies": {
    "@types/node": "^22.10.7",
    "dependency-cruiser": "^16.9.0",
    "madge": "^8.0.0",
    "ts-node": "^10.9.2",
    "typescript": "^5.3.3",
    "vite": "^6.0.7"
  }
}
