{
  "private": true,
  "scripts": {
    "build": "pnpm -r build",
    "dev": "pnpm -r --parallel dev",
    "lint": "pnpm -r lint",
    "test": "pnpm -r test",
    "clean": "rm -rf node_modules && pnpm -r exec rm -rf node_modules dist coverage .nyc_output tsconfig.tsbuildinfo && rm -rf .pnpm-store"
  },
  "devDependencies": {
    "dependency-cruiser": "^16.9.0",
    "madge": "^8.0.0",
    "ts-node": "^10.9.2",
    "typescript": "^5.7.3"
  }
}
