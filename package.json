{
  "private": true,
  "scripts": {
    "build": "pnpm -r build",
    "dev": "pnpm -r --parallel dev",
    "lint": "pnpm -r lint",
    "test": "pnpm -r test",
    "clean": "pnpm -r clean"
  },
  "devDependencies": {
    "dependency-cruiser": "^16.9.0",
    "madge": "^8.0.0",
    "typescript": "^5.3.3"
  }
}
